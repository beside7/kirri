import styled from "styled-components/native";

export const Container = styled.View({});

export const SelectedItemText = styled.View({
    fontSize: 14
});

export const SelectedItemIcon = styled.Image({
    width: 16,
    height: 16,
    resizeMode: "contain"
});
